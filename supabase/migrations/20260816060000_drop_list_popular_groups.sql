-- 그룹 홈에서 인기 그룹 섹션을 걷어내면서 `list_popular_groups`를 버린다.
--
-- 이 함수의 유일한 호출자는 그룹 홈 로더였다. 상위 3개를 티저로 보여주고 "전체 보기"로
-- 탐색 화면에 넘기는 자리였는데, 이 화면에서 사용자가 하는 일은 대부분 이미 가입한 그룹을
-- 다시 찾는 것이고 새 그룹 탐색은 드물다. 그래서 티저를 없애고 탐색으로 가는 링크만
-- 목록 아래에 남겼다. 남은 호출자가 없으니 공식 탭만 보는 사용자까지 매번 물던 쿼리도
-- 같이 사라진다.
--
-- 탐색 화면은 `discover_groups`를 쓴다. 그쪽 정렬에도 인기도가 들어가므로 "인기 그룹"이라는
-- 개념 자체가 사라지는 것은 아니다.
drop function if exists public.list_popular_groups(integer);
