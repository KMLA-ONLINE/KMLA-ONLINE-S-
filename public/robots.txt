# 로그인해야 볼 수 있는 서비스라 검색 결과에 실릴 내용이 없다. 그래서 색인은 막는다.
#
# 다만 막는 수단은 여기의 Disallow가 아니라 `X-Robots-Tag: noindex` 헤더다
# (`vercel.json`). 카카오톡·Facebook 같은 링크 미리보기 수집기는 robots.txt를 따르므로,
# Disallow로 막으면 색인과 함께 공유 카드까지 사라진다. noindex는 "가져가도 되지만
# 검색 결과에는 싣지 말라"는 뜻이라 카드만 남는다.
#
# 크롤링 자체를 막아야 할 경로가 생기면 여기에 적는다.
User-agent: *
Disallow:
