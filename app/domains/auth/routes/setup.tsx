import { AuthCard } from "~/domains/auth/components/auth-card";

export default function SetupPage() {
  return (
    <AuthCard
      title="프로필 설정"
      description="가입 신청에 필요한 정보를 받는 화면입니다."
    />
  );
}
